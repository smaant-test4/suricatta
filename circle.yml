dependencies:
  pre:
    - sudo apt-get update && sudo apt-get install -y postgresql leiningen
    - su postgres -c "createuser -p 5434 -d -s root"
    - createdb -p 5434 test

test:
  override:
    - DB_PORT=5434 lein test-all
