version: 2
jobs:
  build:
    executorType: docker
    containerInfo:
      - image: clojure:lein-2.7.1
    workDir: ~/suricatta
    steps:
      - type: checkout

      - type: shell
        command: echo YAY!
